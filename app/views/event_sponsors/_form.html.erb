<%= form_for(@event_sponsor) do |f| %>
  <% if @event_sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_sponsor.errors.count, "error") %> prohibited this event_sponsor from being saved:</h2>

      <ul>
      <% @event_sponsor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= @sponsor.id %>
    <%= f.hidden_field :sponsor_id, :value => @sponsor.id %>
  </div>
  <div class="field">
    <%= f.label :event %><br>
    <%= f.collection_select(:event_id, @upcoming_events, :id, :name) %>
  </div>
    <%= f.submit %>
<% end %>
